services:
  - type: web
    name: waiver-sentry-app
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: "3000"
      - key: NODE_VERSION
        value: "20"
